require_relative('card.rb')
class CardGame


  def checkforAce(card)
    if card.value = 1 #should have an extra =
      return true
    else
      return false
    end
  end

  dif highest_card(card1 card2) #should have a comma after card1
  if card1.value > card2.value  # should be def not dif
    return card.name #no .name does not exist
  else
    card2
  end
end
end # extra end not needed

def self.cards_total(cards)
  total # total is not doing anything it needs to equal 0
  for card in cards
    total += card.value
    # needs an end on this line
    return "You have a total of" + total # needs the total to be in the string with in a #{}
  end
end
